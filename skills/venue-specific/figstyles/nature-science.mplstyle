# Nature / Science / Cell / PNAS / eLife: Arial/Helvetica, clean, compact
# Nature single column: 89 mm (3.5 in), Double column: 183 mm (7.2 in)
# Science single column: 85 mm, Double column: 174 mm
# Min font size: 5pt at final size, DPI: 300+, Format: EPS/PDF/TIFF

text.usetex          : False
font.family          : sans-serif
font.sans-serif      : Arial, Helvetica, Arial Unicode MS, Hiragino Sans GB, PingFang HK, Heiti TC, Noto Sans CJK SC, WenQuanYi Micro Hei, Microsoft YaHei, SimHei, DejaVu Sans
font.size            : 7
mathtext.fontset     : stixsans

figure.figsize       : 3.5, 2.6
figure.dpi           : 150
savefig.dpi          : 300
savefig.format       : pdf
savefig.bbox         : tight
savefig.pad_inches   : 0.02

# Embed TrueType fonts so PDF text layer survives (default Type-3 outlines
# break pdftotext / search / accessibility / downstream LLM grep). 42 = TrueType.
pdf.fonttype         : 42
ps.fonttype          : 42

axes.labelsize       : 7
axes.titlesize       : 8
axes.linewidth       : 0.5
axes.formatter.use_mathtext : True
axes.spines.top      : False
axes.spines.right    : False

xtick.labelsize      : 6
ytick.labelsize      : 6
xtick.major.width    : 0.5
ytick.major.width    : 0.5
xtick.direction      : out
ytick.direction      : out

lines.linewidth      : 1.0
lines.markersize     : 4

legend.fontsize      : 6
legend.frameon       : False
legend.handlelength  : 1.2

# Okabe-Ito (matches the _default.md guide). Nature spans every domain, so no
# single cycle fits — when brain seeds a domain guide (physics/biology/...),
# that guide's palette is ground truth: update this prop_cycle line to match
# at seed time, before the first illustrator_write spawn.
axes.prop_cycle      : cycler('color', ['0072B2', 'D55E00', '009E73', 'CC79A7', 'F0E442', '56B4E9', 'E69F00'])

grid.linewidth       : 0.3
grid.alpha           : 0.4
